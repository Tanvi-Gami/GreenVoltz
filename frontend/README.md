# GreenVoltz Frontend

This is the scaffold for the GreenVoltz frontend (Phase 1).

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Toggle mocks in `.env.local`:

```
VITE_USE_MOCKS=true
VITE_API_BASE_URL=http://localhost:8000
```
